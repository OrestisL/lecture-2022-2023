using UnityEngine;
using TMPro;

//rename file and class to Homework1_nmxxxxx
public class Homework1Template : MonoBehaviour
{
    //Create a ray from the main camera inwards when either click is pressed, that hits only specific layers
    //If ray hits something, find all surrounding objects in the same layers
    //Using a text mesh pro, display amount of hit objects 

    //set variables here (camera for raycasting, radius, layers, etc)

    private void Start()
    {
        //initialize variables if needed
    }

    private void Update()
    {
        //check for click

        //cast ray

        //check if ray hit

        //if ray hit, find surroundings

        //instantiate display text prefab, set text to amount of objects found (ignoring self)
        //(use "text ="  on text mesh pro object)
    }
}
